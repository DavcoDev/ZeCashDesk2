<header>
    <nav class="navbar navbar-inverse ">
        <div class="container">
            <div class="active">
                <a class="navbar-header" style="width: 20%" href="{{ path('base') }}">
                    <img src="{{ asset('images/logo_projet.png') }}" alt="logo ZeCashDesk"
                         class="img-rounded" style="width:60% ; margin-left: 20%"></a>
            </div>
            <div id="navbar" class="collapse navbar-collapse">
                <ul class="nav navbar-nav navbar-right">
                    <li><p class="navbar-brand">
                            Bonjour {% if is_granted('ROLE_USER') %}{{ app.user.username }}{% endif %}</p></li>
                    {% if is_granted('ROLE_USER') %}
                        <li><a class="navbar-brand" href="{{ path('logout') }}">Déconnexion</a></li>
                    {% endif %}
                    <li><p id="heure" class="navbar-text"></p></li>
                    <li><a class="navbar-brand" href="{{ path('base') }}">Ze Cash Desk</a></li>
                </ul>
            </div>
        </div>
    </nav>
</header>
