<header>
    <nav class="navbar navbar-inverse ">
        <div class="container">
            <div class="active">
                <a class="navbar-header" href="#"><img src="{{ asset('images/logo_projet.png') }}" alt="logo ZeCashDesk"
                                                       class="img-rounded" style="width:25%"></a>
            </div>
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                        aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>
            <div id="navbar" class="collapse navbar-collapse">
                <ul class="nav navbar-nav navbar-right">
                    <li><p class="navbar-text">Bonjour {% if is_granted('ROLE_USER')%}{{ app.user.username }}{% endif %}</p></li>
                    <li></li>
                    <li><p id="heure" class="navbar-text">{{ "now"|date("d/m/Y") }}</p></li>
                    <li><a class="navbar-brand" href="#">Ze Cash Desk</a></li>
                </ul>
            </div><!--/.nav-collapse -->
        </div>
    </nav>
</header>
