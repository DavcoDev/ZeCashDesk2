{% extends 'base.html.twig' %}

{% block body %}
    <h1>Création d'un mouvement de fonds</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('cash_desk_index') }}">Retour à la liste principale</a>
        </li>
    </ul>
{% endblock %}
