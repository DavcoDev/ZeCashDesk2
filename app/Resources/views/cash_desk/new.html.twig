{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-md-offset-3 col-md-6">
        <br>
        <p class="label label-default center-block" style="font-size:  x-large">Création d'une transaction</p><br>
        <div class="row">
            <div class="col-md-offset-3 col-md-6">

                {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" value="Créer"/>
                {{ form_end(form) }}
                <br>
                <ul>
                    <li>
                        <a href="{{ path('cash_desk_index') }}">Retour à la liste principale</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
{% endblock %}
