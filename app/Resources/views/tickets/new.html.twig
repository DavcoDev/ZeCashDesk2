{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-md-offset-3 col-md-6">
        <br>
        <p class="label label-default center-block" style="font-size:  x-large">Création d'un Ticket</p><br>
        <div class="row">
            <div class="col-md-offset-3 col-md-6">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" value="Create"/>
                {{ form_end(form) }}
                <br>
                <ul>
                    <li>
                        <a href="{{ path('tickets_index') }}">Back to the list</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
{% endblock %}
